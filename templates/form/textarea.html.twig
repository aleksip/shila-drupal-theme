{%
  set classes = [
    'form-textarea',
    resizable ? 'resize-' ~ resizable,
    required ? 'required',
  ]
%}
{% set wrapper_attributes = wrapper_attributes.addClass('form-textarea-wrapper') %}
{% set attributes = attributes.addClass(classes) %}

{% include '@molecules/form/classy-textarea/classy-textarea.html.twig' %}
