{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% if breadcrumb %}
  {% include "@molecules/navigation/breadcrumb/breadcrumb.html.twig"
    with {
      'title': 'Breadcrumb'|t
    }
  %}
{% endif %}
