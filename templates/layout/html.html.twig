{%
  set body_classes = [
    logged_in ? 'user-logged-in',
    not root_path ? 'path-frontpage' : 'path-' ~ root_path|clean_class,
    node_type ? 'page-node-type-' ~ node_type|clean_class,
    db_offline ? 'db-offline',
  ]
%}
{% set attributes = attributes.addClass(body_classes) %}
{% set head_title = head_title|safe_join(' | ') %}
{% set skip_to_main_content_text = 'Skip to main content'|t %}

{% include '@organisms/layout/classy-html/_classy-html.html.twig' %}
