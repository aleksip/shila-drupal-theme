{%
  include '@molecules/field/classy-field/demo/classy-field-demo.twig' with {
    'label_hidden': true,
    'attributes': create_attribute({'class': ['field', 'field--node-body']}),
    'items': [
      {
        'attributes': create_attribute({'class': ['field__item']}),
        'content':
          include('@atoms/content/paragraph.twig', with_context = false) ~
          include('@atoms/content/paragraph.twig', with_context = false) ~
          include('@atoms/content/paragraph.twig', with_context = false),
      }
    ]
  } only
%}
