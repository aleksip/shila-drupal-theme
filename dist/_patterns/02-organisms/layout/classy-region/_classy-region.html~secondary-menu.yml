# Hidden pseudo-pattern variant that can be included in other patterns.

attributes:
  Attribute():
    class:
      - region
      - region-secondary-menu
content:
  join():
    - include():
        pattern: organisms-classy-menu-block-html
        with:
          title_attributes:
            Attribute():
              class:
                - visually-hidden
