# Hidden pseudo-pattern variant that can be included in other patterns.

attributes:
  Attribute():
    class:
      - region
      - region-breadcrumb
content:
  join():
    - organisms-classy-block-html-breadcrumb
